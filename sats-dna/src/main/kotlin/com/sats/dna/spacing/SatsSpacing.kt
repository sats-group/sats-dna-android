package com.sats.dna.spacing

import androidx.compose.ui.unit.dp

object SatsSpacing {
    /** A spacing of 4 dp. */
    val xxs = 4.dp

    /** A spacing of 8 dp. */
    val xs = 8.dp

    /** A spacing of 12 dp. */
    val s = 12.dp

    /** A spacing of 16 dp. */
    val m = 16.dp

    /** A spacing of 24 dp. */
    val l = 24.dp

    /** A spacing of 32 dp. */
    val xl = 32.dp

    /** A spacing of 64 dp. */
    val xxl = 64.dp
}
